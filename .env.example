# Email — use Zoho (recommended) or Resend
# Zoho Mail (e.g. info@northernfacilitiesgroup.ca) — use app password if 2FA is on
ZOHO_EMAIL=
ZOHO_APP_PASSWORD=
# Optional: Zoho SMTP (defaults: smtp.zoho.com, 465)
# ZOHO_SMTP_HOST=smtp.zoho.com
# ZOHO_SMTP_PORT=465

# Resend (alternative to Zoho)
RESEND_API_KEY=

# Contact form recipient (where Contact / Quote / Walkthrough emails are sent)
CONTACT_EMAIL=info@northernfacilitiesgroup.ca

# Analytics (optional)
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Site URL (for OG images and sitemap)
NEXT_PUBLIC_SITE_URL=https://northernfacilitiesgroup.ca

# Deals pipeline (quote + walkthrough → your app/CRM)
# POST URL: Zapier/Make/n8n webhook, or your app’s API (e.g. create deal)
# Optional: API key for auth (e.g. "Bearer YOUR_KEY" or "Api-Key YOUR_KEY")
DEALS_PIPELINE_WEBHOOK_URL=
DEALS_PIPELINE_API_KEY=
